public class Plain {
    private PlainProducer plainProducer;
    private String model;
    public Plain(PlainProducer plainProducer, String model){
        this.plainProducer = plainProducer;
        this.model = model;
    }

    @Override
    public String toString() {
        return "Plain{" +
                "plainProducer=" + plainProducer +
                ", model='" + model + '\'' +
                '}';
    }
}
